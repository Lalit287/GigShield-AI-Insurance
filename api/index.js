import app from '../backend/src/app.js';

/**
 * Vercel Serverless Function entry point
 * This bridge connects the Express app from the backend directory to Vercel Functions.
 */
export default app;
